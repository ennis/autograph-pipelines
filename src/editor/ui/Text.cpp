//#include <editor/ui/Text.hpp>

#include <SkCanvas.h>
#include <SkPaint.h>

namespace ui
{
	/*void Text::render(HUDRenderer& renderer, glm::vec2 size) 
	{
		auto cv = renderer.canvas;
		cv->save();
		SkPaint paint;
		paint.setStyle(SkPaint::kFill_Style);
		paint.setColor(SkColorSetRGB(0, 0, 0));
		paint.setTextSize(textSize);
		paint.setTextAlign(SkPaint::kLeft_Align);
		paint.setSubpixelText(true);
		paint.setAntiAlias(true);
		cv->drawText(text.c_str(), text.size(), 0.0f, textSize, paint);
		cv->restore();
	}*/
}