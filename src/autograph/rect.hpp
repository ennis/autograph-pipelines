#ifndef RECT_HPP
#define RECT_HPP

#include <cmath>

/*struct Box1D {
  unsigned xmin;
  unsigned xmax;

  unsigned width() const { return std::abs((float)(xmax - xmin)); }
};

struct Box2D {
  unsigned xmin;
  unsigned ymin;
  unsigned xmax;
  unsigned ymax;

  unsigned width() const { return std::abs((float)(xmax - xmin)); }
  unsigned height() const { return std::abs((float)(ymax - ymin)); }
};

struct Box3D {
  unsigned xmin;
  unsigned ymin;
  unsigned zmin;
  unsigned xmax;
  unsigned ymax;
  unsigned zmax;

  unsigned width() const { return std::abs((float)(xmax - xmin)); }
  unsigned height() const { return std::abs((float)(ymax - ymin)); }
  unsigned depth() const { return std::abs((float)(zmax - zmin)); }
};*/

#endif
